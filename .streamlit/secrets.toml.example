# 사용법: 이 파일을 복사해 같은 폴더에 `secrets.toml` 로 저장하고 실제 값을 채우세요.
# (.streamlit/secrets.toml 은 .gitignore 처리됨 — 절대 GitHub에 올리지 마세요)

[auth]
redirect_uri = "http://localhost:8501/oauth2callback"
# 아래 cookie_secret 은 임의 생성된 값입니다. 그대로 써도 되고, 새로 생성해도 됩니다.
# 생성: python -c "import secrets; print(secrets.token_hex(32))"  → 나온 값을 아래에 붙여넣기
cookie_secret = "여기에_랜덤_문자열_생성해서_넣기"

[auth.google]
client_id = "여기에_구글_OAuth_클라이언트_ID_붙여넣기"
client_secret = "여기에_구글_OAuth_클라이언트_Secret_붙여넣기"
server_metadata_url = "https://accounts.google.com/.well-known/openid-configuration"

# 관심종목 저장용 Supabase (Phase 3)
# Supabase 대시보드 → Project Settings → API 에서 복사
[supabase]
url = "https://여기에_프로젝트.supabase.co"
key = "여기에_anon_public_key_붙여넣기"
